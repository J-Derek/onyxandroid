# ===================================
# Onyx Frontend Environment Variables
# ===================================

# Backend API URL (leave empty for same-origin)
VITE_API_URL=

# LAN Host for Party Mode sharing
# Set this to your local network IP and port for party invitations to work across devices.
# Example: VITE_LAN_HOST=192.168.1.100:3000
# Find your IP: Run `ipconfig` on Windows or `ip addr` on Linux/Mac
VITE_LAN_HOST=
